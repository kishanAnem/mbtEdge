import { metadata as comingSoonMetadata } from './metadata';

export const metadata = comingSoonMetadata;

export default function ComingSoonLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}